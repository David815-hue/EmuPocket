Original prompt: Oye una pregunta sera posible crear un emulador ejecutado puramente en la web de Gameboy Color donde pueda usar pues las teclas y tambien que en juegos como pokemon pueda usar el mouse para escoger ataques, que yo pueda subir la room o no se bueno tu sabreas, dime si es viable

- 2026-03-12: Se inicia un prototipo web desde cero en una carpeta vacia.
- 2026-03-12: Objetivo de esta iteracion: crear la interfaz del emulador, carga local de ROM, controles de teclado, overlay clicable y una capa adaptadora lista para conectar un core real de GBC.
- 2026-03-12: Se crea una pagina estatica con canvas 160x144, UI tipo consola, carga local de ROM, save slots en localStorage, overlay de menu clicable y hooks window.render_game_to_text/window.advanceTime.
- 2026-03-12: Se integra el core de GameBoy-Online como vendor local para emulacion GBC en navegador.
- 2026-03-12: La UI ahora carga ROMs reales, manda inputs al core, usa save states del emulador y traduce clics del overlay a secuencias de botones pensadas para menus de batalla estilo Pokemon.
- 2026-03-12: Prueba automatizada en navegador con Chrome local: la pagina carga, una ROM de prueba (`cpu_instrs.gb`) inicia correctamente, el canvas renderiza, el overlay responde y los save slots no generan errores de consola.
- 2026-03-12: Se rediseña la interfaz como consola virtual mas pulida con botonera fisica clicable, temas visuales, scanlines y paneles de configuracion persistentes.
- 2026-03-12: El overlay ahora funciona por contextos (`battle-root`, `battle-fight`, `party`, `bag`, `nav`) y encadena transiciones automaticas para hacer mas precisa la seleccion con mouse en juegos como Pokemon.
- 2026-03-12: Se agrega una capa propia de cheats RAM persistentes basada en escrituras periodicas a memoria del core.
- 2026-03-13: Se integra soporte dual GB/GBC y GBA con deteccion por extension y cambio visual de sistema.
- 2026-03-13: La UI ahora comunica mejor el sistema activo, el perfil de controles y la diferencia entre save states (GB/GBC) y SRAM (GBA).
- 2026-03-13: Se agrega selector manual de sistema (`Auto`, `GB/GBC`, `GBA`) para poder cambiar tambien toda la skin visual antes de cargar una ROM.
- 2026-03-13: Se separan overlays y presentacion visual entre GB/GBC y GBA, incluyendo transicion visual de cambio de sistema y branding distinto por consola.
- 2026-03-13: La skin de Game Boy Color se rehace usando una referencia de comunidad, manteniendo canvas real, botones funcionales y animaciones de presion.
- 2026-03-13: El modo GBA cambia hacia una carcasa inspirada en Game Boy Advance SP y se agrega un flujo opcional de "presiona boton para iniciar" con interruptor persistente en configuracion.
- 2026-03-13: Se detecta que el modo GBA mezclaba dos skins distintas; se rehacen proporciones y posiciones para acercarlo mucho mas al mockup SP con tapa, bisagra y base separadas.
- 2026-03-13: Se mapean las clases del mockup SP (`screen-body`, `screen-border`, `pad-container`, `speaker-holder`, `power-button`, `select/start labels`) y se deja un bloque final de overrides para que el look GBA se parezca mucho mas al HTML/CSS de referencia.
- 2026-03-13: Se mejora la UI superior con una barra de sesion mas limpia y accesos de import/export/reset visual.
- 2026-03-13: Se agregan import/export de saves en JSON, remapeo de teclas persistente desde la UI, filtros visuales (`lcd`, `pixel`, `soft`, `boost`) y controles tactiles responsive configurables.
- 2026-03-13: Se integra una primera base de Nintendo DS usando DS Anywhere/WebMelon con runtime descargado del sitio publicado (`wasmemulator.js/.wasm`), deteccion de `.nds`, selector de sistema DS y doble pantalla.
- 2026-03-13: Se rehace la skin de Nintendo DS usando una maqueta tipo clamshell con tapa superior, bisagra, pantalla tactil inferior, botonera X/Y/A/B y cruceta especifica de DS.\n- 2026-03-13: Se extiende el remapeo y el input DS con botones X/Y reales via WebMelon.\n- 2026-03-13: Verificacion visual con Playwright propio en http://127.0.0.1:4173/?fresh=ds-mockup-4 y captura completa en output/ds-mockup-full.png; el cliente del skill cargo la app pero no pudo pulsar el toggle DS por viewport del runner.
- 2026-03-13: Se crean archivos separados por sistema para flujo de mockups de comunidad: `gbc.html`+`gbc.css`, `gba.html`+`gba.css`, `ds.html`+`ds.css`; cada HTML queda con su `data-system` fijo y `index.html` apunta a `gbc.css` por defecto.
- 2026-03-13: Se integran los 3 mockups (GBC/GBA/DS) a la app con paginas dedicadas funcionales (mockup visible + hooks ocultos para `app.js`) y `index.html` pasa a ser un lanzador de sistemas.
- 2026-03-13: Se restaura `index.html` como UI principal del emulador usando `styles.css`; los mockups separados (`gbc.html`, `gba.html`, `ds.html`) quedan como referencias aisladas para no romper el flujo principal.
- 2026-03-17: `index.html` se rehace como hub limpio con `iframe` para cargar `gbc.html`, `gba.html` y `ds.html` sin alterar sus DOM/CSS originales; la prioridad es que los mockups se vean exactamente igual y sigan usando sus paginas funcionales ya existentes.
- 2026-03-17: El hub de `index.html` precarga los 3 mockups y solo alterna visibilidad para que el cambio de sistema sea instantaneo y estable en pruebas automatizadas; ademas `gba.html` deja de depender del logo remoto de Wikimedia y usa `assets/gba-sp-logo.svg`.
- 2026-03-17: Se corrige el error del lanzador sin emulacion. `index.html` vuelve a ser la app principal y ahora usa `#console-shell-host` con shell dinamico dentro del mismo DOM.
- 2026-03-17: `src/app.js` ahora renderiza shells por sistema (GBC/GBA/DS), refresca referencias DOM cuando cambia la consola y conserva la carga de ROM desde `index.html`.
- 2026-03-17: Se genera `mockups-main.css` como version scoped de los mockups para poder usar sus estilos dentro del `index` sin reventar `styles.css`; se ajusta el fit de GBA/DS para que entren en la escena principal.
- 2026-03-17: Verificacion en navegador con Playwright: el `index` cambia entre GBC/GBA/DS, carga `fixtures/cpu_instrs.gb` desde `#rom-input`, reporta `rom_loaded` en `#status-line` y no muestra errores de consola/pageerror.
- 2026-03-17: Se corrige un crash reportado en `syncCanvasResolution` cuando algun canvas aun no existia. Ahora `app.js` re-renderiza/refresca el shell si faltan canvases y sale sin romper si alguno sigue nulo.
- 2026-03-17: Se cambian query strings de `styles.css`, `mockups-main.css` y `src/app.js` en `index.html` para invalidar cache del navegador y evitar que el usuario siga cargando el JS roto anterior.
- 2026-03-17: Re-prueba dirigida en viewport similar al del usuario: el `index` muestra de nuevo la consola GBC, carga `cpu_instrs.gb` y el boton `#power-btn` cambia el estado a `power_start` sin errores de consola/pageerror.
- 2026-03-17: Se arregla la visibilidad de controles DS en `index`: los overrides ahora estilizan `button.game-button`, `button.set-button` y la cruceta, no solo `div.*`, por lo que los botones vuelven a verse como en el mockup.
- 2026-03-17: Se arregla la interaccion del d-pad GBA en `index`: se crean hitboxes reales para `d-up/right/down/left`, `bindInputButton` acepta `pointerdown` y `click`, y el shell GBA se escala con `zoom` para que no quede gigante ni fuera del viewport.
- 2026-03-17: Se quita el efecto de "recuadro" del host principal (`console-shell-host`) dejando fondo transparente y overflow visible; verificado con Playwright que DS vuelve a mostrar botones y que en GBA un click en `left` actualiza `#status-line` a `Ultima entrada: left`.
- 2026-03-17: Se corrige el fit de la pantalla GBA dentro del `index`: el override scoped de `mockups-main.css` ahora hace que `#screen-wrap`, `#screen`, `#screen-hint` y `#menu-overlay` ocupen toda el area `.screen` del mockup SP.
- 2026-03-17: Se invalidan de nuevo los assets de `index.html` (`styles.css`, `mockups-main.css`, `src/app.js`) con `?v=20260317-gba-screenfix` para evitar cache viejo del navegador.
- 2026-03-17: Verificacion visual con Playwright en `http://127.0.0.1:4173/?fresh=gba-screenfix-full3`: tras forzar el toggle GBA por DOM, `#system-chip` reporta `Sistema GBA`, la captura `output/gba-screenfix-page.png` muestra la pantalla ocupando correctamente el panel del SP y no hubo errores de consola/pageerror.
- 2026-03-17: Se corrige el D-pad de DS en `index`: `mockups-main.css` recupera la geometria y sombras exactas del mockup original (`cross-box`, `top/left/bot/right-cross`) pero manteniendo los `button` para input real.
- 2026-03-17: Se invalidan assets de `index.html` a `?v=20260317-ds-dpadfix` para evitar cache viejo del navegador.
- 2026-03-17: Verificacion con Playwright en `http://127.0.0.1:4173/?fresh=ds-dpadfix`: el click en `.left-cross` actualiza `#status-line` a `Ultima entrada: left` y la captura `output/ds-dpadfix-cross.png` muestra la cruceta con el look del mockup.
- 2026-03-17: Se corrige recorte de GBA en algunos navegadores: el ajuste del shell deja de depender solo de `zoom` y ahora usa `transform: scale(...)` con fallback condicional para navegadores que si soportan `zoom`.
- 2026-03-17: Se agrega escalado responsive por altura (`--gba-shell-scale`) para que en viewports mas bajos no se corte la mitad inferior de la consola.
- 2026-03-17: Se actualizan versiones de assets en `index.html` a `?v=20260317-gba-layoutfix`; validado con Playwright (`788x560`) que `gamepad-body` es visible y sin errores de consola/pageerror.
- 2026-03-17: Se rediseña el layout general del `index` alrededor de una escena fija de consola: header mas limpio, rail lateral de contexto, nuevo marco visual para la consola y una composicion mas enfocada en la pantalla.
- 2026-03-17: Se agrega `focusMode` persistente en `src/app.js`, con boton de entrada/salida, clase `body.focus-mode`, cierre rapido con `Escape` y ocultamiento de UI externa mientras la consola queda como protagonista.
- 2026-03-17: Se unifica el shell renderizado en `src/app.js` mediante `buildFixedShellTemplate(system)`: GBC, GBA y DS ahora usan la misma carcasa base y solo cambia el contenido de pantalla/estado del sistema.
- 2026-03-17: Se aisla la carcasa fija de los estilos viejos de mockup cambiando las clases de shell a `console-shell--fixed` con `data-shell-system`, evitando herencia accidental de `mockups-main.css`.
- 2026-03-17: Verificacion con Playwright en `?fresh=fixed-shell-final-pass`: sin errores de consola/pageerror, `focus-mode` activo correctamente, y capturas generadas en `output/fixed-shell-gbc-clean.png`, `output/fixed-shell-gba-final.png`, `output/fixed-shell-ds-final.png` y `output/fixed-shell-focus-final.png`.
- 2026-03-17: Se reajusta el zoom visual de GBA y DS en `mockups-main.css` con reglas tambien por ancho de viewport para que los mockups entren mejor en pantallas estrechas y bajas sin cortar botonera ni bisagra.
- 2026-03-17: El boton de fullscreen deja de depender del shell completo: `src/app.js` ahora mueve los canvas reales a `#fullscreen-stage`, activa `body.emulation-fullscreen`, intenta `requestFullscreen()` nativo sobre ese stage y devuelve los canvas al mockup al salir.
- 2026-03-17: `styles.css` agrega reglas para `fullscreen-stage`, con canvas GBA/GBC maximizados por relacion de aspecto y Nintendo DS apilando `#ds-top-screen` y `#ds-bottom-screen` en columna dentro del viewport.
- 2026-03-17: Verificacion Playwright final (`?fresh=zoom-fullscreen-pass5-page` y cliente del skill en `output/web-game-pass5`): sin errores de consola/pageerror; GBA fullscreen ocupa `788x525.3` en viewport `788x560`, y DS fullscreen separa ambos canvas con rects `y=21.0` y `y=289.0`.
